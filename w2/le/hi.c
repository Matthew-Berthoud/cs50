#include <stdio.h>
#include <cs50.h>

int main(void)
{
    string s = "HI!";
    printf("%i %i %i %i\n", s[0], s[1], s[2], s[3]);

    // Printing the NUL character as well
        // NUL character (\0) deliniates where one string ends and the other begins
    // this implicitly converts chars to their asci values
    // STRING = ARRAY OF CHARS
}